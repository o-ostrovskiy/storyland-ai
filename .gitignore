# StoryLand AI - Git Ignore File

# ========================================
# Environment & Secrets
# ========================================
# Prefix-match EVERY .env variant. MYS-623 (2026-07-21): a local backup named
# `.env.bak-2026-07-20` was swept into a commit on this PUBLIC repo by
# `git add -A`, exposing a live GOOGLE_API_KEY and LANGFUSE_SECRET_KEY. It
# matched none of the patterns below it -- not `.env` (exact), not
# `.env.local` (exact), not `.env.*.local` (needs the .local suffix), and not
# `*.bak` (needs a .bak SUFFIX; that file ended in `-2026-07-20`). Enumerating
# suffixes cannot work, because the next backup will be named something else.
# Match the prefix. Already-tracked files are unaffected by gitignore, so a
# committed .env*.example stays tracked.
.env*
*.pem
*.key
service-account*.json
credentials.json

# ========================================
# Python
# ========================================
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.hypothesis/
.pytest_cache/
pytest_cache/

# ========================================
# IDEs & Editors
# ========================================
# VSCode
.vscode/
*.code-workspace

# PyCharm
.idea/
*.iml
*.iws

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim
*.swp
*.swo
*~

# Emacs
*~
\#*\#
.\#*

# ========================================
# OS
# ========================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# ========================================
# Project Specific
# ========================================
# Langfuse evaluation pipeline generated files
evaluation/results/
evaluation/langfuse_datasets.json
evaluation/metrics.json
# Note: evaluation/trend_report.md is tracked in git to show evaluation trends over time

# ADK generated files
.adk/

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp

# Agent cache
.agent_cache/
.session_cache/

# Database files (if using SQLite for dev)
*.db
*.sqlite
*.sqlite3

# Jupyter Notebooks
.ipynb_checkpoints/

# ========================================
# Cloud & Deployment
# ========================================
# Terraform
*.tfstate
*.tfstate.*
.terraform/

# Docker
docker-compose.override.yml

# ========================================
# Documentation Build
# ========================================
docs/_build/
docs/_static/
docs/_templates/
site/

# ========================================
# Miscellaneous
# ========================================
# Backup files
*.bak
*.backup

# Archive files
*.zip
*.tar.gz
*.rar
AGENTS.md

# Any .env variant, incl. backups (.env.bak-*): a live-secret file in the
# worktree is one careless git-add from a public-repo leak (2026-07-21).
.env*
